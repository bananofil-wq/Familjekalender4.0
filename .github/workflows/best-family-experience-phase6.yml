name: Best family experience phase 6

on:
  workflow_dispatch:
  push:
    branches: [ feature/best-family-experience ]
    paths:
      - 'scripts/apply_schedule_series_ids.py'
      - '.github/workflows/best-family-experience-phase6.yml'

permissions:
  contents: write

jobs:
  apply-and-build:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/best-family-experience
      - name: Apply schedule series IDs
        run: python3 scripts/apply_schedule_series_ids.py
      - name: Commit source changes
        run: |
          if git diff --quiet; then
            echo 'No source changes needed'
          else
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add app/src/main/java/se/familjekalender/app/ExactCalendarScreen.kt app/src/main/java/se/familjekalender/app/SchoolScheduleDialog.kt
            git commit -m 'Use stable series IDs for work and school schedules'
            git push origin HEAD:feature/best-family-experience
          fi
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Verify approved seasonal images
        run: python3 scripts/verify-season-images.py
      - name: Compile debug APK
        run: gradle :app:assembleDebug --stacktrace
