name: Apply running progress v2

on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/apply_running_progress_v2.py'
      - '.github/workflows/apply-running-progress-v2.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply running progress v2
        run: python3 scripts/apply_running_progress_v2.py
      - name: Commit source change
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/se/familjekalender/app/RunningProgressCard.kt
          git commit -m "Improve running progression summary"
          git push
