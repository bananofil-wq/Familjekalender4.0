name: Apply day popup delete

on:
  push:
    paths:
      - 'scripts/add-day-popup-delete.py'
      - '.github/workflows/apply-day-popup-delete.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply popup delete patch
        run: python3 scripts/add-day-popup-delete.py
      - name: Commit source change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/se/familjekalender/app/ExactCalendarScreen.kt
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Allow deleting events from day popup"
            git push origin main
          fi
