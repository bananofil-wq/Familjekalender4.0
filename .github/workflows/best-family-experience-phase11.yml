name: Best family experience phase 11

on:
  workflow_dispatch:
  push:
    branches: [ feature/best-family-experience ]
    paths:
      - 'scripts/apply_final_ux_cleanup.py'
      - '.github/workflows/best-family-experience-phase11.yml'

permissions:
  contents: write

jobs:
  apply-and-build:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/best-family-experience
      - name: Apply final UX cleanup
        run: python3 scripts/apply_final_ux_cleanup.py
      - name: Commit source changes
        run: |
          if git diff --quiet; then
            echo 'No source changes needed'
          else
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add app/src/main/java/se/familjekalender/app/FamilyAssistantScreen.kt
            git commit -m 'Remove duplicate today summary from assistant'
            git push origin HEAD:feature/best-family-experience
          fi
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Verify approved seasonal images
        run: python3 scripts/verify-season-images.py
      - name: Compile debug APK
        run: gradle :app:assembleDebug --stacktrace
