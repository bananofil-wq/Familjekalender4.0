name: Apply calendar enhancements (legacy)

# Legacy source-patching workflow disabled. Its calendar offset assumptions no longer
# match the current UI and it must not interfere with the Clean/Sport redesign.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy calendar patch is intentionally disabled."
