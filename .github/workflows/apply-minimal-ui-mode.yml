name: Apply selectable minimalist UI

on:
  push:
    branches: [ main ]
    paths:
      - 'app/src/main/java/se/familjekalender/app/MinimalCalendarScreen.kt'
      - 'scripts/apply_minimal_ui_mode.py'
      - '.github/workflows/apply-minimal-ui-mode.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply minimalist UI integration
        run: python3 scripts/apply_minimal_ui_mode.py

      - name: Commit integration
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/se/familjekalender/app/MainActivity.kt
          git commit -m "Add selectable minimalist interface"
          git push
