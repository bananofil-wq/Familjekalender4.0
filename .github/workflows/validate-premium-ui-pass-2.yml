name: Validate Premium UI Pass 2

on:
  push:
    branches: [ feature/premium-ui-pass-2-20260916 ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  premium-ui:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feature/premium-ui-pass-2-20260916
      - name: Apply premium UI pass
        run: python3 scripts/apply_premium_ui_pass_2.py
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace
      - name: Persist validated source changes
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No source changes to persist"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/se/familjekalender/app
          git commit -m "Applicera premium UI-pass 2"
          git push
